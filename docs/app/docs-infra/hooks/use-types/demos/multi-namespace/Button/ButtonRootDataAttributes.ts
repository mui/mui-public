export enum ButtonRootDataAttributes {
  /**
   * Present when the button is disabled.
   */
  disabled = 'data-disabled',
  /**
   * Present when the button is pressed.
   */
  pressed = 'data-pressed',
}
