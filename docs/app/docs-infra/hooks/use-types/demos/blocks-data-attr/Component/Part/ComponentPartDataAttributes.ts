export enum ComponentPartDataAttributes {
  /**
   * Present when the component has a title.
   */
  title = 'data-title',
  /**
   * Present when the component is disabled.
   */
  disabled = 'data-disabled',
}
