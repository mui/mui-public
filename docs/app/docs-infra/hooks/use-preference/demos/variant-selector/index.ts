import { createDemo } from '@/functions/createDemo';
import { VariantSelector } from './VariantSelector';

export const DemoUsePreferenceVariantSelector = createDemo(import.meta.url, VariantSelector);
