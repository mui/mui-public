import { createDemo } from '@/functions/createDemo';
import { CollapsibleTransform } from './CollapsibleTransform';

export const DemoCollapsibleTransform = createDemo(import.meta.url, CollapsibleTransform, {
  name: 'Collapsible Code Block with Transform',
  slug: 'collapsible-code-block-with-transform',
});
