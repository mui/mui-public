import { createDemo } from '@/functions/createDemo';
import { IndentCode } from './IndentCode';

export const DemoIndentCode = createDemo(import.meta.url, IndentCode, {
  name: 'Indent Shifting',
  slug: 'indent-shifting',
});
