import { createDemo } from '@/functions/createDemo';
import { CredentialsCode } from './CredentialsCode';

export const DemoCodeHighlighterCodeTransformedAddition = createDemo(
  import.meta.url,
  CredentialsCode,
);
