import { createDemo } from '@/functions/createDemo';
import { ConditionalComments } from './ConditionalComments';

export const DemoChunkConditionalComments = createDemo(import.meta.url, ConditionalComments);
