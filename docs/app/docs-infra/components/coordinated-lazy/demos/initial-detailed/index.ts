import { createDemo } from '@/functions/createDemo';
import { InitialDetailedChart } from './InitialDetailedChart';

export const DemoChunkInitialDetailed = createDemo(import.meta.url, InitialDetailedChart);
