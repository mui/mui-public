import { createTypes } from '@/functions/createTypes';
import * as hastUtils from '@mui/internal-docs-infra/pipeline/hastUtils';

export const TypesHastUtils = createTypes(import.meta.url, hastUtils);
