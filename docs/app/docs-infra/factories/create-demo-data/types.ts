import { createTypes } from '@/functions/createTypes';
import { createDemoData } from '@mui/internal-docs-infra/createDemoData';

export const TypesCreateDemoData = createTypes(import.meta.url, createDemoData);
