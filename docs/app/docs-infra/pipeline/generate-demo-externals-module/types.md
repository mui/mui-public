# Generate Demo Externals Module

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate docs-infra/pipeline/generate-demo-externals-module'

## API Reference

### filterRuntimeExternals

Filters out type-only imports from externals since they don't exist at runtime.
This is essential for client-side code where type imports are stripped during compilation.

**Parameters:**

| Parameter | Type        | Default | Description |
| :-------- | :---------- | :------ | :---------- |
| externals | `Externals` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = Externals;
```

### findServerOnlyExternals

Inspects collected externals and returns the list of module paths that are
server-only. An empty array means the demo's dependencies are safe to inline
into the client bundle.

Pass the unfiltered externals (i.e. before `filterRuntimeExternals`) so that
side-effect imports like `import 'server-only';` — which have no bound names
and would otherwise be dropped — are still detected.

**Parameters:**

| Parameter | Type        | Default | Description |
| :-------- | :---------- | :------ | :---------- |
| externals | `Externals` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string[];
```

### generateDemoExternalsModule

Generates static imports and runtime external values for demo entries.

**Parameters:**

| Parameter | Type                                 | Default | Description |
| :-------- | :----------------------------------- | :------ | :---------- |
| options   | `GenerateDemoExternalsModuleOptions` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = Promise<GeneratedExternalsModule>;
```

### generateResolvedExternals

Generates both import statements and resolved externals object
Returns the import statements and the externals as a JavaScript object

`existingNames` are identifier names already in scope at the injection site
(e.g. existing imports or top-level declarations in the destination file).
They are seeded into the conflict-resolution set so injected imports get
aliased to a unique name when they would collide.

**Parameters:**

| Parameter      | Type        | Default | Description |
| :------------- | :---------- | :------ | :---------- |
| externals      | `Externals` | -       | -           |
| existingNames? | `string[]`  | -       | -           |

**Return Value:**

| Property            | Type                     | Description |
| :------------------ | :----------------------- | :---------- |
| `imports`           | `string[]`               | -           |
| `resolvedExternals` | `Record<string, string>` | -           |

### injectImportsIntoSource

Injects import statements into source code after 'use client' directive.
Handles proper placement of imports in client components.

**Parameters:**

| Parameter   | Type       | Default | Description |
| :---------- | :--------- | :------ | :---------- |
| source      | `string`   | -       | -           |
| importLines | `string[]` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### ServerOnlyDemoExternalError

**Static Methods:**

```typescript
function isError(error: unknown): boolean;
```

Indicates whether the argument provided is a built-in Error instance or not.

**Constructor Parameters:**

| Parameter    | Type       | Default | Description                                      |
| :----------- | :--------- | :------ | :----------------------------------------------- |
| modules      | `string[]` | -       | Server-only modules found in the source graph.   |
| dependencies | `string[]` | -       | Source URLs discovered before validation failed. |

**Properties:**

| Property     | Type       | Modifiers | Description                                      |
| :----------- | :--------- | :-------- | :----------------------------------------------- |
| modules      | `string[]` | readonly  | Server-only modules found in the source graph.   |
| dependencies | `string[]` | readonly  | Source URLs discovered before validation failed. |
| name         | `string`   | -         | -                                                |
| message      | `string`   | -         | -                                                |
| stack?       | `string`   | -         | -                                                |
| cause?       | `unknown`  | -         | -                                                |

## Additional Types

### GenerateDemoExternalsModuleOptions

```typescript
type GenerateDemoExternalsModuleOptions = {
  /** Source entries whose external imports should be collected. */
  entries: DemoEntry[];
  /** Names already declared in the destination module. */
  existingNames?: string[];
  /** Source loader. Defaults to the local filesystem loader. */
  loadSource?: LoadSource;
  /** Maximum relative-import depth. */
  maxDepth?: number;
};
```

### GeneratedExternalsModule

```typescript
type GeneratedExternalsModule = {
  /** Static import statements for the runtime dependencies. */
  imports: string[];
  /** JavaScript expression containing resolved external-module values. */
  valueExpression: string;
  /** Runtime external imports collected from the source graph. */
  externals: Externals;
  /** Unique source URLs used by the demo. */
  dependencies: string[];
};
```

## External Types

### LoadSource

```typescript
type LoadSource = (url: string) => Promise;
```
