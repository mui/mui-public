# Precompute Demo

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate docs-infra/pipeline/precompute-demo'

## API Reference

### precomputeDemo

Loads and processes demo source entries without a factory wrapper.

**Parameters:**

| Parameter | Type                    | Default | Description |
| :-------- | :---------------------- | :------ | :---------- |
| options   | `PrecomputeDemoOptions` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = Promise<PrecomputedDemo>;
```

## Additional Types

### DemoEntry

```typescript
type DemoEntry = {
  /** Variant name used as the key in the returned code. */
  name: string;
  /** Source file URL. */
  url: string;
  /** Displayed file name. Defaults to the file name in the URL. */
  fileName?: string;
  /**
   * Language used to pick the grammar, overriding the file extension. Set this
   * when the extension understates the syntax, such as JSX in a `.js` file.
   */
  language?: string;
  /** Named export that provides the demo component. */
  namedExport?: string;
};
```

### PrecomputedDemo

```typescript
type PrecomputedDemo = {
  /** Processed source keyed by entry name. */
  code: Code;
  /** External imports collected from every source file. */
  externals: Externals;
  /** Unique source URLs used by the demo. */
  dependencies: string[];
};
```

### PrecomputeDemoOptions

```typescript
type PrecomputeDemoOptions = {
  /** Source entries to process. */
  entries: DemoEntry[];
  /** Source loader. Defaults to the local filesystem loader. */
  loadSource?: LoadSource;
  /** Source parser used for syntax highlighting. */
  sourceParser?: Promise<ParseSource>;
  /** Source transformations applied before parsing. */
  sourceTransformers?: SourceTransformer[];
  /** HAST transformations applied after parsing. */
  sourceEnhancers?: SourceEnhancer[];
  /** Serialized source format. Defaults to `hastCompressed`. */
  output?: 'hast' | 'hastJson' | 'hastCompressed';
  /** Maximum relative-import depth. */
  maxDepth?: number;
  /** Whether the default source loader follows relative imports. */
  includeDependencies?: boolean;
};
```

## External Types

### LoadSource

```typescript
type LoadSource = (url: string) => Promise;
```

### SourceEnhancer

```typescript
type SourceEnhancer = (
  root: { data?: unknown | undefined },
  comments: {} | undefined,
  fileName: string,
) => { data?: unknown | undefined } | Promise;
```
