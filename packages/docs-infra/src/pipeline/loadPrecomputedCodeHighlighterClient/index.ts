// This is the export format a webpack / Turbopack loader expects

import { loadPrecomputedCodeHighlighterClient } from './loadPrecomputedCodeHighlighterClient';

export default loadPrecomputedCodeHighlighterClient;
