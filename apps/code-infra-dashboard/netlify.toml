[build]
  publish = "apps/code-infra-dashboard/build"

  # Default build command.
  command = "pnpm -F code-infra-dashboard build"

[dev]
  command = "pnpm -F code-infra-dashboard start"
  targetPort = 3000

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "functions"
