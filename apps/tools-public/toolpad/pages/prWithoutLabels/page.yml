# yaml-language-server: $schema=https://raw.githubusercontent.com/mui/toolpad/v0.13.0/docs/schemas/v1/definitions.json#properties/Page

apiVersion: v1
kind: page
spec:
  title: PR without labels
  alias:
    - tn213hge
  displayName: PR without labels
  content:
    - component: Text
      name: text
      layout:
        columnSize: 1
      props:
        mode: markdown
        value:
          "This page has moved to the code-infra-dashboard:
          [PR without labels](https://code-infra-dashboard.onrender.com/github-triage?view=prs-without-labels)"
  display: shell
