# yaml-language-server: $schema=https://raw.githubusercontent.com/mui/toolpad/v0.13.0/docs/schemas/v1/definitions.json#properties/Page

apiVersion: v1
kind: page
spec:
  title: Closed vs. opened issues
  alias:
    - 2413lvx
  displayName: Closed vs. opened issues
  content:
    - component: Text
      name: text
      layout:
        columnSize: 1
      props:
        mode: markdown
        value:
          "This page has moved to the code-infra-dashboard:
          [Closed vs. opened issues](https://code-infra-dashboard.onrender.com/kpis)"
  display: shell
