# yaml-language-server: $schema=https://raw.githubusercontent.com/mui/toolpad/v0.13.0/docs/schemas/v1/definitions.json#properties/Page

apiVersion: v1
kind: page
spec:
  title: Issue without product scope
  alias:
    - y923hoo
  displayName: Issue without product scope
  content:
    - component: Text
      name: text
      layout:
        columnSize: 1
      props:
        mode: markdown
        value:
          "This page has moved to the code-infra-dashboard:
          [Issue without product scope](https://code-infra-dashboard.onrender.com/github-triage?view=issues-without-product-scope)"
  display: shell
