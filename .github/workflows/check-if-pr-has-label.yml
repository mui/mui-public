name: PR has label

on:
  pull_request:
    types: [opened, reopened, labeled, unlabeled]

permissions: {}

jobs:
  test-label-applied:
    permissions:
      contents: read
    uses: ./.github/workflows/check-if-pr-has-label-base.yml
