name: PR Package releases
on:
  pull_request:
  push:
    branches:
      - master
      - next
      - v*.x

permissions: {}

jobs:
  continuous-releases:
    name: CI Releases
    uses: ./.github/workflows/ci-base.yml
